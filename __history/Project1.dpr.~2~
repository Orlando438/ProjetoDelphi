program Project1;

uses
  Vcl.Forms,
  uFormCadBase in 'Bases/uFormCadBase.pas' {FormCadBase};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TFormCadBase, FormCadBase);
  Application.Run;
end.
